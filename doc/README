Try the following to make OptionMatrix documentation

make pdf
make html
make epub
make zip
make xml
make info
make text
make docbook
make htmlhelp
make frames
make pdf2
make html2
make source_html

Check spelling, diction and style with
make spelling
make diction
make style
Try 'sudo apt-get install aspell diction' to install packages
under GNU/Linux using the apt-get package manager.

Do HTML link checking with
make linkchecker
Try 'sudo apt-get install linkchecker' to install packages
under GNU/Linux using the apt-get package manager.

make dvi is disabled in the Makefile.am
do to the lack of EPS (Encapsulated PostScript) images.
These images are very large.

To create PostScript and DVI documentation try
make png2eps ; make ps
See files optionmatrix.ps optionmatrix.dvi
See EPS images in directory ./images

For 'make png2eps' to work ImageMagick mogrify must be installed
Try 'sudo apt-get install imagemagick' to install packages
under GNU/Linux using the apt-get package manager.
